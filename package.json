{
  "name": "matching-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=/memory-game/ --config vite.config.js && node replace-paths.cjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@rollup/plugin-replace": "^6.0.1",
    "vite": "^5.4.17"
  }
}
